{# Jinja template for Kong request-validator plugin #}
plugin:
  name: request-validator
  config:
    parameter_schema: {{ config.parameter_schema|default('{}') }}
    body_schema: {{ config.body_schema|default('{}') }}
    verbose_response: {{ config.verbose_response|default(false) }}
